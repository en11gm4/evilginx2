# Улучшения скрытия Evilginx2

## Обзор изменений

Данный документ описывает внесенные изменения для уменьшения обнаруживаемости фреймворка Evilginx2 системами безопасности.

## 🔧 Внесенные изменения

### 1. Удаление идентифицирующих HTTP заголовков

**Проблема:** Фреймворк устанавливал легко идентифицируемый заголовок `X-Evilginx-Home`

**Решение:**
- Закомментированы строки установки заголовка `req.Header.Set(p.getHomeDir(), o_host)`
- Изменена функция `getHomeDir()` для возврата менее очевидного имени

**Файлы:** `core/http_proxy.go`

### 2. Улучшение генерации имен cookie

**Проблема:** Предсказуемый алгоритм генерации имен cookie (8 случайных символов + хеш)

**Решение:**
- Используются реалистичные имена: `session`, `sid`, `auth`, `token`, `csrf`, `xsrf`, `state`, `nonce`
- Переменная длина суффикса (4-7 символов)
- Формат: `{name}_{suffix}`

**Примеры:** `session_a1b2c`, `auth_x7y8z9w`

### 3. Изменение алгоритма хеирования cookie

**Проблема:** Предсказуемый формат хеша `xxxx-xxxx`

**Решение:**
- Новый алгоритм генерации имен сессионных cookie
- Формат: `{prefix}_{suffix}_{randomPart}`
- Используются реалистичные суффиксы: `id`, `token`, `hash`, `key`, `sig`, `auth`, `sess`, `csrf`

**Примеры:** `sess_id_a1b2c3d4e5f`, `auth_token_x7y8z9w0q1r2`

### 4. Изменение URL паттернов

**Проблема:** Характерные пути `/s/{session_id}` и `/s/{session_id}/{js_id}`

**Решение:**
- Случайный выбор из реалистичных путей:
  - Редиректы: `/redirect/`, `/auth/`, `/callback/`, `/return/`
  - JavaScript: `/assets/`, `/static/`, `/js/`
- Динамическая смена паттернов при каждом запуске

### 5. Обфускация JavaScript кода

**Проблема:** Характерный JavaScript код с функцией `getRedirect()`

**Решение:**
- Замена fetch на XMLHttpRequest
- Добавление случайных параметров URL
- Различные методы редиректа
- Случайные задержки
- Обфускация логики

### 6. Улучшение заголовков безопасности

**Проблема:** Полное удаление заголовков безопасности (подозрительно)

**Решение:**
- Добавление реалистичных заголовков безопасности
- Случайный выбор заголовков (66% вероятность)
- Сохранение некоторых заголовков для естественности

**Добавленные заголовки:**
- `Content-Security-Policy`
- `Strict-Transport-Security`
- `X-XSS-Protection`
- `X-Content-Type-Options`
- `X-Frame-Options`
- `Referrer-Policy`
- `Permissions-Policy`

### 7. Скрытие конфигурационных файлов

**Проблема:** Очевидная директория `~/.evilginx`

**Решение:**
- Использование менее очевидных путей:
  - `~/.cache/session/`
  - `~/.local/session/`
  - `~/.config/session/`
  - `~/.data/session/`
- Случайный выбор при каждом запуске

## 📊 Сравнение артефактов

| Артефакт | До | После |
|----------|----|------|
| Заголовок X-Evilginx-Home | `X-Evilginx-Home: host` | Отсутствует |
| Имя cookie | `abcd1234` | `session_a1b2` |
| Сессионный cookie | `a1b2-c3d4` | `sess_token_x7y8z9w0q1` |
| URL путь | `/s/session_id` | `/redirect/session_id` |
| Директория конфига | `~/.evilginx` | `~/.cache/session` |
| Заголовки безопасности | Отсутствуют | Реалистичные |
| JavaScript | Характерный | Обфусцированный |

## 🛡️ Дополнительные рекомендации

### Сетевой уровень
1. **Случайные User-Agent:** Используйте пул реалистичных User-Agent строк
2. **Случайные задержки:** Добавляйте небольшие случайные задержки в запросах
3. **Шумовой трафик:** Генерируйте фоновые запросы для маскировки
4. **TLS Fingerprinting:** Используйте различные TLS конфигурации

### Уровень приложения
1. **Динамическая обфускация:** Регулярно меняйте методы обфускации
2. **Случайные заголовки:** Добавляйте случайные HTTP заголовки
3. **Разнообразие ответов:** Используйте различные HTTP статусы и сообщения
4. **Фейковые эндпоинты:** Создайте фиктивные API эндпоинты

### Уровень файловой системы
1. **Шифрование конфигов:** Используйте шифрование конфигурационных файлов
2. **Случайные имена:** Используйте случайные имена для временных файлов
3. **Очистка следов:** Регулярно очищайте временные файлы и логи
4. **Скрытые атрибуты:** Используйте скрытые атрибуты файлов

## 🔍 Проверка изменений

Для проверки изменений используйте утилиту:
```bash
./detect_artifacts.sh
```

Утилита проверит:
- Отсутствие идентифицирующих заголовков
- Реалистичность имен cookie
- Разнообразие URL паттернов
- Обфускацию JavaScript кода
- Скрытие файловых артефактов

## ⚠️ Важные замечания

1. **Тестирование:** Всегда тестируйте изменения в безопасной среде
2. **Совместимость:** Убедитесь, что изменения не нарушают функциональность
3. **Мониторинг:** Следите за системами обнаружения и адаптируйтесь
4. **Регулярные обновления:** Периодически обновляйте методы скрытия

## 📝 Заключение

Внесенные изменения значительно уменьшают вероятность обнаружения фреймворка автоматизированными системами безопасности. Однако важно помнить, что никакие методы не гарантируют полной скрытности, и следует регулярно обновлять подходы к обходу детекции.